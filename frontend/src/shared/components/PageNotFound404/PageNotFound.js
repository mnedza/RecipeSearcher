import React from "react";

import styles from "./PageNotFound.module.css"

const PageNotFound = () => {
  return <h1 className={`${styles.content} wrapper`}>Page Not Found!</h1>;
};

export default PageNotFound;
